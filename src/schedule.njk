---
layout: page-layout.njk
title: Schedule
description: The Vermont Code Camp schedule includes sessions on a variety of software topics, including programming, databases, web and more.
---


<h1>{{ config.year }} {{ title }}</h1>

<p> {{description}} 
    <em>Please read our <a href="/conduct">Code of Conduct</a> and <a href="/diversity">Diversity Statement</a>.</em>
</p>

<table class="schedule">

    <thead>
        <tr>
        {% for th in sessionize.schedule.head %}
            <th scope="col" class="{{th.type}}">
                {{ th.title }} 
                {% if th.subtitle %}
                    <small class="room">{{ th.subtitle }}</small>
                {% endif %}
            </th>
        {% endfor %}
        </tr>
    </thead>

    <tbody>
    {% for row in sessionize.schedule.body %}

        <tr>
        {% for cell in row %} 
            {% if cell.type == "timespan" %}
                <td id="{{ cell.timeSlug }}" class="{{ cell.type }}" scope="row">
                    {{ cell.title }}
                </td>
            {% elif cell.type == "plenumSession" %}
                <td colspan="5"> {{ cell.title }} </td>
            {% elif cell.type == "session" %}
                <td> 
                    <a href="{{ cell.title_link }}">{{ cell.title }}</a>
                    <small class="speaker">
                    {% for speaker in cell.speakers %}
                        <a href="{{ speaker.link }}">{{ speaker.name }}</a>
                    {% endfor %}
                    </small>
                </td>
            {% else %}
                <td>
                    {# No session scheduled for this timeslots. Blank cell. #}
                </td>
            {% endif %}
        {% endfor %} 
        </tr>

    {% endfor %}
    </tbody>

</table>

